#!markdown

Markdown Block can be initiated via #!markdown

This is the markdown block
# And This is Header

#!pwsh

# This is Code Block
# Code Block can be initiated with #!pwsh
$PSVersionTable

#!markdown

# This is another markdown header with list
- List 1
- List 2
- List 3

#!pwsh

# This is Sample Code Block
$a = random(1..10)
$b = random(100..1000)
$c = $a + $b

Write-Host "a + b is $c"

#!pwsh

# This is code block initiated from git browser
for ($i = 1;$i <= 10;$i++) {
  Write-Host "This is $i"
}
